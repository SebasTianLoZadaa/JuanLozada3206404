import random

def mayor_y_frecuencia():
    lista = [random.randint(1, 100) for _ in range(20)]
    print("Lista:", lista)
    mayor = max(lista)
    frecuencia = lista.count(mayor)
    print(f"El número mayor es {mayor} y aparece {frecuencia} veces.")

mayor_y_frecuencia()